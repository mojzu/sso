version: '3'

services:
    # # ark_auth service.
    # ark_auth:
    #     build: ark_auth
    #     depends_on:
    #         - postgres
    #     restart: always
    #     environment:
    #         DATABASE_URL: "postgres://guest:guest@postgres:5432/ark_auth"
    #         SERVER_BIND: "0.0.0.0:9000"
    #     ports:
    #         - "9000:9000"

    # PostgreSQL service.
    postgres:
        image: postgres:11.3
        restart: always
        network_mode: "host"
        environment:
            POSTGRES_USER: guest
            POSTGRES_PASSWORD: guest
        ports:
            - "5432:5432"
